#include <string>
#include <bits/stdc++.h>
#include <algorithm>
#include <stack>

using namespace std;


int main(){
    int t;
    cin >> t;
    int a[2005];
    while(t--){
        int n,k;
        cin >> n >> k;
        for (int i=0;i<n;++i){
            cin >> a[i];
        }
        map<int,pair<int,vector<int>>> m;
        for (int i=0;i<n;++i){
            for (int j=0)
        }
    }
    
    return 0;
}